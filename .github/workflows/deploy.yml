name: CI/CD Pipeline Simulation

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Simulated CI step
        run: |
          echo "Build and test simulation successful."

      - name: Simulated deployment step
        run: |
          echo "Simulated deployment completed."
